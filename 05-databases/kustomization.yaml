apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The base is MariaDB Operator (+ Redis for cache). The old self-managed
# Galera was removed from the repo, the migration is complete.
resources:
  - ./mariadb-operator
  - ./redis
